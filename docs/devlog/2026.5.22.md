## 2026.5.22 and 2026.5.23

现在准备开始推进adviskv的主线任务了。

目前看来，这个项目已经进行了75%了。

目前接下来要做的，就是对于一些细节的完善了。

- 关于meta的单测实现完善

- 关于sdm的单测实现完善

- 接入benchmark测一下性能（后续考虑进行优化

- 完善文档细节和代码质量细节

### meta单测

- 测试创建db之后，没有sdm_client，导致db被删除（检验返回码），然后查询db是否存在。

- 测试创建db之后，sdm_client那边返回fail，导致db被删除（检验返回码），然后查询db是否存在。

- 测试创建db一切正常，然后查询db是否存在

- 测试创建table之后，没有sdm_client，失败了，table的状态会是update，检测err_msg

- 测试创建table之后，sdm_client_->call_place_table返回失败了，table的状态会是update，检测err_msg

- 测试创建table成功了，get_table的两种方法是没有问题的

- 测试创建table成功后，drop_table的table.state == TableState::DELETED，table.state == TableState::DROPPING，!sdm_client_，status.fail()，以上情况都要测到

- 测试创建table成功了，然后删除table成功了，然后再创建一个同名的table后，get_table用name和id，分别找到当下的table和之前的table

### sdm单测

#### background

##### heartbeat

- 直接就测试一下当这个NODE从ONLINE到SUSPECT到OFFLINE的过程，以及最后OFFLINE的时候，上面的replica是否状态改成了LOST

- 测试下一个SUSPECT的NODE变成ONLINE，然后那些Replica的这个状态也进行了这个更新。

- 测试一下，当所有的node都是online，结果这个SDM崩溃了，然后重新启动。然后在刚开始的时候，这个 node他们虽然这个心跳时间和当前比超过了这个标记为这个offline的这个时间，但是依旧不会被标记成offline还是online。

- 还是上述的这个情况，SDM崩溃重新启动之后，然后之前node是offline的这个那些node，然后它们在这个限制的这个时间里面，然后发出的这个心跳，但是它们是会更改的，状态是可以进行更新的，检测这个。

另外在这里想单测的时候，突然意识到了两个事情。首先第一个就是，如果这个SDM这边重启之后的话，那这个所有的这个node上的这个replica就都要被这个标为这个lost了，然后之后才能再重新过那个恢复过来，这个中间会有一个抖动期，并不太好。所以说那个搞了个小操作，优化了一下。

另外还有一点就是没有处理标记，当node是online的时候，也需要搞一下这个Replica的这个操作。

##### route_update

我觉得这个路由表的更新的这个task 不是特别的复杂，所以光检测这个函数就可以了吧check_shard_route

- 检测一下，当这个leader的数量是小于一的时候，会调用这个函数delete_shard_route

- 检测一下，当这个呃leader的数量等于一的时候是否是正常的

- 检测一下，当leader的数量是大于一的时候，但是term是有多个相同的，这个时候会返回一个状态码error。

- 检测一下，当leader的数量是大于一的时候，但是term是各不相同的。此时是按照我们的预期来走的。

##### table_reconciler

分成创建table和删除table两种

继承TableReconciler然后去单独测一下reconcile_once吧。

发现之前对于tablePhase的状态定义有一点混乱了:

目前统一一下，这边进入了FAILED就代表不会进入重试了。如果中间出现了一些失败的状态，会进行判断，如果是可重试了，例如在创建table里，那么状态就还是CRAETING，不会变成FAILED，只会更新error_msg。

创建table

- 正常完整流程是否可以走完一遍

- 当ensure_replica_metadata的选出来node有问题，需要检查table的状态应该是FAILED和他的msg。（可以直接继承NodeSelector，然后select操作直接返回ERROR那种）

- 当ensure_replica_metadata的build_replicas有问题时，也检测table的状态和msg

- 当ensure_storage_replicas的时候create_replica返回可以重试和不可以重试的错误的检测

- refresh_storage_replica_info的get_replica_info返回可以重试和不可以重试的错误的检测



删除table

这个就大差不差了，仿照创建的思路来搞

#### client

这个单测貌似不需要，毕竟别的地方也都测试到这里了。

#### manager

才发现manager这边都注释掉了，都没有用上在sdm里面。

#### model

##### MemoryMetaStore



##### PersistentMetaStore

#### persist

#### selector (node_selector)

#### service
## 2026.5.24

目前剩余的内容如下：

- [ ]  sdm的单测
  
  - [ ] i_sdm_metastore的持久化的测试
  
  - [ ] sdm_runtime_index的测试
  
  - [ ] sdm_store的测试

- [ ] 端到端的测试

- [ ] 接入benchmark 



#### i_sdm_metastore



#### runtime_index

- 检测下正常流程是否有没有问题就好了。

#### sdm_store

如果测试起来不太方便的话，可以吧runtime_index改成unique_ptr那种，然后继承runtime_index，这样测试起来应该方便一些。

- 检测一下正常的流程是否有没有问题

- return maybe_repair_runtime_index(runtime_index_.on_replica_delete(*old_ptr));让一个返回值失败，然后走rebuild，看看是否rebuild正确。 由于别的思路也是这样，就不用反复测试了。

// 阿巴很久没有搞了

之前把sdsdk搞完了，还有很简陋的sdm_store。

目前的进度大概好像是: 
- 有关sdm侧的store的并发还没实现
- storage侧目前决定搞个raft，然后走leader读写，保障最后的强一致性读写


现在raft感觉搞了有百分之6,70吧，现在的TODO是:

- timer还没有实现

// 4.30
现在把raftnode从replica抽离出来了
timer设定删掉了，直接用的backgroundtask + 一个计数器trigger。

//5.2
目前快照更新了，还有关于raft_node内部，直接先全部用锁搞上了，关于可以用队列去处理从而不加锁的操作以后再考虑吧。
接下来要处理
- 关于persist_engine内部的代码细节
- 关于raft_sender的重构
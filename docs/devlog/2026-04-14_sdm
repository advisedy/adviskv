//4.14 
目前又发现，这个组内的设计中，sdm会有两个后台任务，一个是CapacityChecker，一个是ReplicaScheduler。
这两个，前者是用来检测副本的容量是否合规的，如果少了就创建Replica，但是没有挂载node，后者就直接删去replica。
然后后者是用来检测replica是否挂载node的，如果没有挂载的话就进行调度。
然后node的来源不是通过RPC发送请求，而是通过心跳上传给sdm的资源池，然后调度就在资源池里面选择node。
那么调度之后，并不会主动给node发送请求。
而是在心跳请求的回应里面，对于这个node，sdm会返回一个 这个node应该持有的replicas列表，然后node本地会进行一个diff比较，如果它没有的话，就会调用本地的add_replica。
之后storage侧本地构建完之后，sdsdk会内部存下来这个replica的相关信息（例如这个node给外部提供的ip地址），然后下一次心跳的时候上报给sdm，然后sdm得到了这个replica的
相关信息之后（例如node给外部提供的ip地址），会存到自己的路由表里面。

其中关于这个存到路由表实际是一个RouteUpdater后台任务完成的:
- 不是 Heartbeat handler 直接把它写进 routing cache
- 而是 Heartbeat 先把 replica 的最新状态写进 StateStore
- 然后由后台任务 RoutingUpdater 把 StateStore 里的这些 replica state 重新汇总生成 routing cache

至此以上内容，完成了这个创建application的环节。

所以对应到当前的这个项目里面的createTable的话，我觉得也参考这种架构比较的合适，而不是说这个placement_service去导致调用RPC，显得sdm这边一直在给外部调用RPC的样子，
确实感觉这种要么sdm被收到消息然后交给后台任务逐个处理，或者说就是在给出response的时候带上自己的消息比较的合适一些。

所以接下来主要是改写sdm内部的逻辑，还有sdsdk的实现。

而且关于之前的这个架构设计，应该是要改变了，operation的设计貌似有点鸡肋了放到现在思考的话。
以后在设计的时候还是先考虑清楚现在的组内的架构设计是啥样的吧。

// wan shang

所以对于createTable来说，就应该直接创建出来table，然后就够了。

// 4.15 
目前对于架构要有一次大改了，之前的manager应该都不用了，关于sdm的操作其实service + background_task + 底层的sdm_store就基本能干的差不多了。
之前设计的operation， plan有点过度设计了。


// 晚上:

目前正在写心跳task。
这个task主要是用来检测各个node的心跳的。
由于node是和resource pool绑定在一起的，所以我们直接枚举pool里面的nodes。

对于一个node来说，首先他们在注册的时候会用到我们sdm这边的sdsdk，这个里面应该会内置一个心跳发送，设置成每一秒就发送消息。
然后这边对于心跳来说，主要有两个，一个是接受Handler侧，一个是这边的检测。 
检测方会对于node的状态进行判断，第一版的内容应该只是涉及是否将这个node去掉。
然后Handler侧那边就是实时更新Node的状态，至于检测交给了检测方心跳Task去做。
 
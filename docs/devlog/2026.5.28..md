## 2026.5.28

- e2e的测试场景思考Ï

目前先打算着重e2e的测试开发：

首先先规定好这个e2e的测试的一些场景的情况，毕竟我们已经有了gtest，并且gtest那边也可以做到一些具体崩溃点的测试，所以我们的e2e的测试应该着重于有关RPC链路，或者是实际一些的内容。

大概应该分成下面这几种：

- 基础链路（有关RPC的部分，确保实际有效）
  
  - 创建db，table，putkey，getkey，delkey，删除db，删除table。再次创建db，创建table。跑一个完整的流程下来差不多就可以了。（finish

- 崩溃场景
  
  - storage（应该这个storage会是重点，毕竟这里有raft集群）
    
    - 一个follower落后了之后，直接靠log追上（finish
    
    - 一个follower落后了之后，靠快照+log追上
    
    - leader崩溃了后切主，然后再回来之后要成为follower追上
  
  - meta
    
    - 貌似没有太好测试的，这个崩溃更侧重是持久化的内容。
  
  - sdm
    
    - meta给sdm发送创建table的消息之后，停掉，然后再恢复，meta 不需要处理，sdm这边之后会建table成功
    
    - 同上，删除操作也是这样

- 持久化场景
  
  - storage
    
    - 崩溃多数派，然后leader选举不出来，然后重启，数据还在。并且可以继续操作
  
  - meta
    
    - 崩溃后，持久化的数据都还在。并且可以继续操作
  
  - sdm
    
    - 崩溃后，持久化的数据都还在。并且可以继续操作





ok，昨天晚上放松开耍，今天下午认真wor。



update: 这边好像意识到，proto这边有些语义还没有收紧，比较混乱，以后要记得搞





# TODO: 统一服务与工具启动参数语义

## 背景结论

当前项目 CLI 语义分裂：e2e 使用位置参数读取 conf，Storage/SDM/Meta 也通过位置参数或默认值读取 conf，而 bench 已经开始使用统一的 `--name=value` 解析。后续应统一到 `--conf=...`，避免每个入口继续手写参数解析。

## 执行计划

1. e2e 先改成 `--conf=...`，接入 `tools/common/ArgParser`。
   
   - 状态：已完成。
   - 目标命令形态：`e2e_client --conf=conf/e2e_client.yaml --case=basic_kv`。
   - 验收：旧的位置参数 conf 不再接受；pytest helper 同步使用 `--conf=...`。

2. Storage / SDM / Meta 后续统一改成 `--conf=...`。
   
   - 状态：TODO，暂不执行。
   - 短期改动：三个服务入口只解析 `--conf=...`，删除位置参数 conf。
   - 验收：`storage conf/storage.yaml`、`sdm conf/sdm.yaml`、`meta conf/meta.yaml` 均应失败；`storage --conf=conf/storage.yaml` 等新形式成功。

3. 修掉 conf 初始化失败后仍继续执行的问题。
   
   - 状态：TODO，暂不执行。
   - 短期改动：Storage / SDM / Meta 配置文件缺失、YAML 解析失败、关键配置项缺失时，入口必须直接返回非 0。
   - 验收：传入不存在的 conf 或非法 YAML，进程不得继续初始化 RPC、Raft、Storage engine 或后台线程。

4. 删除 SDM / Meta 的默认 conf。
   
   - 状态：TODO，暂不执行。
   - 短期改动：`sdm`、`meta` 不再默认读取 `conf/sdm.yaml`、`conf/meta.yaml`，必须显式传 `--conf=...`。
   - 验收：无参数启动必须失败并打印明确错误。

## 风险点

- 这是不兼容 CLI 变更，所有脚本、pytest helper、文档示例都要跟进。
- `ArgParser` 当前只覆盖基础类型解析，服务入口如果只需要 `--conf=...` 足够；不要把 YAML 配置读取逻辑塞进 CLI parser。
- Storage/SDM/Meta 的配置初始化失败处理要按入口逐个核对，不能只改参数格式。





记得现在是bench的输出内容有e2e。要改下TODO

关于PersistEngine持久化里面链路的上锁的串行思考。
